# The THIN venv. Nothing here may pull in torch, transformers, biotite or numpy's
# scientific stack: the droplet has 3.9 GB shared with five other apps, and the web layer
# only reads a precomputed SQLite file and serves static files.
#
# If an import error on the droplet tempts you to add torch here, the correct fix is to
# precompute the value into the database instead.
Flask==3.1.3
gunicorn==26.0.0
