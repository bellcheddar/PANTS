[pytest]
# pythonpath rather than `pip install -e .`: editable installs silently do nothing on this
# machine under Python 3.14 (site.py skips .pth files in a UF_HIDDEN directory), so the
# package resolves to nothing and every test errors on import.
pythonpath = .
testpaths = tests
addopts = -q
